# mockups/ — Sweet Outside. Savage Inside.

Drop the generated **mockup images** here (marketing previews only — never the
print file). Expected set for this design:

```
sweet-outside-savage-inside__mockup__back__black-tee-model.png       (primary — back print on model)
sweet-outside-savage-inside__mockup__front__black-tee-flatlay.png    (front option, flat-lay)
sweet-outside-savage-inside__mockup__back__black-tee-lifestyle.png   (lifestyle)
sweet-outside-savage-inside__mockup__tote__natural-canvas.png        (tote version)
```

Each is produced from the matching prompt in `../prompts/`. On every mockup,
verify the text reads **Sweet Outside. Savage Inside.** exactly — no drift.

> Placeholder describing the deliverable. Actual JPG/PNG mockups go here with
> the names above (see automation/naming-rules.md).
